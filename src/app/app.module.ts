// AppModule intentionally removed for this focused admin UI task.
// The app is started via `bootstrapApplication` in `src/main.ts` which launches
// the standalone AdminDashboard and Reports components.
export const APP_MODULE_REMOVED = true;
